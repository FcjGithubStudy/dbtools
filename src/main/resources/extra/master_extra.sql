-- -------------------------------------------------
-- 【开头说明】
-- 此文件撰写【额外的扩展】sql，采用通用写法，兼容所有数据库
-- 所有的【字段名】前后,均用【占位符@】标记，在运行时会更根据数据库动态替换成【`】或【"】符号。
-- 扩展sql将会在校验器逻辑全部结束后被调用执行。（尚未开发，待定）
-- -------------------------------------------------


















-- -------------------------------------------------
-- 【结尾再次说明，方便查看】
-- 此文件撰写【额外的扩展】sql，采用通用写法，兼容所有数据库
-- 所有的【字段名】前后,均用【占位符@】标记，在运行时会更根据数据库动态替换成【`】或【"】符号。
-- 扩展sql将会在校验器逻辑全部结束后被调用执行。
-- -------------------------------------------------
